# URL base del API TrueShop.
# En desarrollo se deja '/api' y el proxy de Vite (vite.config.ts) lo redirige a http://localhost:5150.
# Para apuntar directo al backend, descomenta la siguiente línea:
# VITE_API_URL=http://localhost:5150/api
